# Create a clean 3.11 env
conda create -n cinemetric311 python=3.11 -y
conda activate cinemetric311

# Good practice: latest build tools
python -m pip install -U pip setuptools wheel

# Install your deps
pip install -r requirements.txt


conda activate cinemetric311
